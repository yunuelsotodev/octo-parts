---
title: {Decision-oriented title — Verb object context, or "Avoid X", or "Use X for Y"}
tags: {prefix}, {concept}, {concept}
# Optional — performance skills only. Omit both for correctness/idiom rules.
# impact: {CRITICAL|HIGH|MEDIUM|LOW}
# impactDescription: {honest consequence, e.g. "prevents stale reads"}
---

## {title}

{WHY — 1-3 sentences. Name the wrong default (what a competent engineer/model does
here without this rule) and its concrete consequence on the BEAM. This is the
highest-signal part: the model generalizes from the reason, not the instruction.}

```elixir
{The canonical way. Production-realistic names — never foo/bar. This single
example is usually all that's needed.}
```

Reference: [{source title}]({url})

<!-- Add an Incorrect/Correct foil ONLY when the wrong way is a real, common trap
     (never a strawman). Keep the diff minimal — same names, only the key line
     changes. Optional: **When NOT to use this pattern:** for real exceptions. -->
