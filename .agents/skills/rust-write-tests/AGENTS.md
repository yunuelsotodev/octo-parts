# Rust Write Tests

This curated skill mirrors `SKILL.md`. When maintaining it, keep the guidance focused on high-value Rust tests, intent-based assertions, flake hunting, and coverage of likely failures.
