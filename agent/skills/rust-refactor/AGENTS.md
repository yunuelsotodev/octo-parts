# Rust Refactor

This curated skill mirrors `SKILL.md`. When maintaining it, keep the guidance focused on incremental Rust refactoring, module decomposition, dead abstraction removal, and type migration.
